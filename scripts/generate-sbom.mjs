import { readFile, writeFile, mkdir } from "node:fs/promises";
const pkg = JSON.parse(await readFile("package.json", "utf8")); const lock = JSON.parse(await readFile("runtime/versions.lock.json", "utf8")); const components = [...Object.entries(pkg.dependencies).map(([name, version]) => ({ type: "library", name, version })), ...Object.entries(lock.components).map(([name, value]) => ({ type: "application", name, version: value.version, licenses: [{ license: { id: value.license } }] }))]; await mkdir("artifacts", { recursive: true }); await writeFile("artifacts/sbom.cdx.json", `${JSON.stringify({ bomFormat: "CycloneDX", specVersion: "1.6", version: 1, metadata: { component: { type: "application", name: "java-lsp-mcp", version: pkg.version } }, components }, null, 2)}\n`);
